# 托管班学生学习管理系统 PRD v1.0

讨论人：杨瑞鑫、杨景奕
形成日期：2026-08-01
来源讨论：[托管班.md](托管班.md)

## 一、背景与目标

托管班老师目前依靠人工/纸质方式跟踪每个学生每天的作业完成情况，效率低、不透明，家长也无法及时了解孩子当天的学习表现。本项目希望做一款轻量的信息化看板工具，帮助托管班老师：

1. 快速给学生分配当日作业
2. 用可视化看板实时跟踪每个学生的任务完成情况
3. 一键生成当日学习总结，方便同步给家长
4. 沉淀历史数据，支持月度回顾

## 二、目标用户与使用场景

- **用户**：
  - **托管班老师**：管理自己名下的托管班级和学生
  - **机构管理员/校长**：创建老师账号，可查看本机构内所有老师的班级、学生及完成情况
- **机构隔离**：系统支持多个托管机构入驻，不同机构之间数据完全不可见，互不感知彼此存在
- **设备**：手机浏览器（H5 网页应用，无需安装 App）
- **场景**：老师在学生到校后逐一打开班级看板，分配/跟踪任务；放学时统一收尾；随时可分享单个学生的当日总结给家长；管理员按需查看机构整体情况

### 两个"班级"概念的区分（重要）

本产品中存在两个不同维度的"班级"，务必区分：

1. **托管班级**：本系统的核心组织单位，即学生放学后所在的托管小组（如"托管1班""托管2班"）。看板、任务分配、放学等全部功能都以**托管班级**为主线组织
2. **学校班级**：学生在学校就读的行政班级（如"六年1班""六年2班"），在托管场景中**仅作为学生卡片上的一个信息属性展示**，不参与任何看板组织、权限或统计逻辑。同一托管班级内的学生，学校班级可以各不相同（因为不同学校班级的学生放学后混编进同一个托管班）

## 三、总体架构

- **形态**：移动端优先的响应式 H5 网页应用，通过链接直接在手机浏览器访问
- **前端**：React + Vite + TypeScript + Tailwind CSS
- **后端**：自建服务，Spring Boot（Java），Docker 容器化部署，包括：
  - 账号认证：Spring Security + JWT（无状态 Token，前后端分离）；账号由机构管理员创建，不支持自助注册
  - 数据库：MySQL（机构、老师、班级、学生、任务、记录等）
  - 对象存储：MinIO（自建，容器化部署，存放学生头像图片）
- **图片海报生成**：前端本地合成（如 html2canvas 一类方案），无需后端渲染服务，降低成本
- **数据隔离**：数据隔离以**托管机构**为边界——同一机构内，管理员可查看所有老师的数据，但老师之间彼此不可见彼此的班级/学生；不同机构之间完全隔离，互不感知。不存在跨账号实时协作场景，因此无需实时跨端同步机制（Realtime），降低了架构复杂度
- **风险提示**：若老师主要通过微信内置浏览器打开链接，部分能力（拍照、保存图片到相册、分享）在微信内置浏览器中可能受限，建议同时支持"复制链接到系统浏览器打开"的引导，或后续视情况评估是否需要小程序化

## 四、账号与权限

层级关系：**托管机构 → 老师 → 托管班级 → 学生**

### 角色说明

| 角色 | 账号创建方式 | 数据可见范围 |
|---|---|---|
| 机构管理员/校长 | 留待实现阶段决定（如后台手动开通），不在本轮 PRD 范围内 | 本机构内所有老师的全部托管班级、学生及完成情况（机构级看板） |
| 老师 | **不支持自行注册**。由所属机构的管理员创建账号并设置初始密码，老师首次登录后自行修改密码 | 仅自己名下的托管班级和学生，看不到本机构内其他老师的数据 |

### 权限规则

- **不提供老师自助注册入口**：若允许老师自行选择/搜索机构再注册，会暴露系统内存在哪些机构，违背机构间"互相隔离、互不感知"的原则。因此老师账号只能由机构管理员创建
- **托管班级由机构管理员创建并指定给某位老师**，老师不能自行新建托管班级；一个托管班级只归属一位老师，不支持多老师共管同一班级
- **班级内的学生名单由负责该班级的老师自行维护**（新增/编辑/删除学生），管理员不参与日常学生名单的增删
- **任务库按机构隔离**：同一机构内所有老师共享一个任务库，任何老师新增的模板机构内全员可见；不同机构之间任务库互不可见

## 五、核心数据模型

| 实体 | 关键字段 |
|---|---|
| 托管机构 Institution | 名称 |
| 老师 Teacher | 账号、所属机构、角色（管理员/普通老师）、所属托管班级列表 |
| 托管班级 Class | 名称、所属机构、所属老师（唯一） |
| 学生 Student | 姓名、头像、所属托管班级、**学校班级（信息属性，如"六年1班"）**、在读状态 |
| 任务库模板 TaskTemplate | 名称、科目分类（语能/数能等）、所属机构（机构内共享）、创建人 |
| 每日任务 DailyTask | 学生、日期、任务内容、是否为定制任务、完成状态 |
| 每日评星 DailyRating | 学生、日期、星级（0.5~5，可空） |
| 班级放学状态 ClassDismissal | 托管班级、日期、是否已放学 |

历史每日任务与评星按天留存，**不会因为进入下一天而被删除**，用于月度统计。

## 六、功能需求

### 6.1 任务库
- 提供任务库管理入口，老师可新增/使用常规作业模板（如"语文作业""数学口算"），按科目分类
- 任务库按机构共享：同一机构内任何老师新增的模板，机构内全员可见；不同机构之间互不可见

### 6.2 学生卡片看板
- 首页按**托管班级分标签页（Tab）**，每个托管班级下学生卡片竖向滑动列表（适配手机操作）
- 每天默认展示托管班级固定花名册的全部在读学生，无需每日签到
- 每个学生卡片显示：姓名、头像、**学校班级**（如"六年1班"，纯信息展示，不参与任何统计/权限逻辑）、当日任务清单（todo 形式，可逐项打勾）

### 6.3 每日任务分配
- 老师从任务库中勾选任务，**批量分配给整个托管班级**
- 分配后可对个别学生单独增减常规任务
- 可为单个学生添加**定制任务**，定制任务显示在常规任务列表之后
- 每天打开看板时，当天任务列表从空白开始重新分配（不会自动带入前一天的任务）
- **按学校班级批量同步**：当老师为某一学生单独新增一项任务时（无论是从任务库单独勾选，还是添加定制任务），系统默认**自动**将同一项任务同步追加给**同一托管班级内、学校班级相同**的其他学生，无需老师确认（因为同校班同学在学校老师那里通常拿到相同的作业）。老师事后可在任意学生卡片上单独删除该项任务，不影响其他学生。同步范围仅限当前托管班级内，不跨托管班级

### 6.4 任务完成与卡片状态
卡片颜色规则，优先级从高到低：
1. **绿色 + "已完成"大印章**：当天全部任务（含定制任务）已勾选完成
2. **红色**：所在托管班级已点击"放学"，且该学生仍有未完成任务
3. **默认色**：其余情况（进行中，未放学）

学生在变红后继续补完任务，卡片会重新变绿（放学不锁定操作，只是当时状态的提醒）。

### 6.5 头像
- 老师点击卡片头像位置，可直接拍照或从手机相册选图上传
- 头像存储于云存储，仅登录老师可见，不开放学生/家长自行上传入口（降低儿童照片隐私风险）

### 6.6 放学按钮
- 每个托管班级独立设置"放学"按钮
- 点击后，仅影响当前托管班级：未完成任务的学生卡片变红
- 放学后老师仍可继续为学生打勾/修改任务状态

### 6.7 当天表现评星
- 卡片右下角显示半星~5星评分入口
- 由老师主观评定（反映学习态度、纪律等综合表现），与任务完成度无直接算法关联
- 选填，不评默认为空，不作为强制流程

### 6.8 月度统计页
- 点击学生卡片上的统计图标，进入该学生本月详情页，展示：
  - 本月完成天数 / 未完成天数
  - 每日完成率趋势折线图
  - 本月平均评星

### 6.9 分享（图片海报）
- 点击学生卡片的分享图标，前端合成一张图片海报，包含：学生头像、当日任务清单（✅/⬜）、当日评星
- 生成后弹出预览，老师可保存图片或调用系统分享，自行发送到微信

### 6.10 定制化任务
- 已在 6.3 中说明：定制任务基于常规任务之上添加，显示顺序在常规任务之后

### 6.11 机构管理员看板
- 管理员登录后可查看本机构下所有老师、所有托管班级及学生的当日完成情况（跨老师的机构级视图），具体展示形式（是否为看板汇总列表、是否支持按老师/班级筛选）留待实现阶段细化
- 管理员可创建老师账号并设置初始密码
- 管理员创建托管班级并指定给某位老师；班级建好后，班内学生名单的日常维护交由该班老师自行操作
- 管理员本身账号的创建方式见「九、待确认的实现细节」

## 七、典型每日流程

1. 老师打开链接，登录后进入托管班级 Tab，看到当天该托管班级学生卡片列表
2. 若当天未分配任务，老师从任务库勾选任务批量下发给全班，再对个别学生调整/追加定制任务
3. 学生到校后，老师在对应卡片上逐项打勾
4. 某学生任务全部完成 → 卡片自动变绿并显示"已完成"印章
5. 放学时老师点击该托管班级"放学"按钮 → 未完成学生卡片变红（仍可继续补勾）
6. 老师视需要为学生评星、上传头像、生成分享海报发给家长
7. 次日打开看板，当天任务列表清空重新开始，历史记录保留，可在月度统计页查看

## 八、非目标（v1.0 不包含）

- 家长端账号/自助查看入口（当前仅老师操作，通过手动分享图片给家长）
- 任务完成度与评星的自动关联算法
- 按任务科目类型细分的月度统计（如"语文完成率 vs 数学完成率"），后续可视反馈迭代

## 九、待确认的实现细节（不影响本轮设计，交由实现阶段决定）

- 托管机构本身及其管理员账号的开通方式（如后台手动开通）
- 机构管理员看板的具体页面形式（汇总列表/筛选方式等）
- 图片海报的具体视觉样式（配色、排版）
- 任务库模板的科目分类的具体枚举值
- **学生转托管班/退托管的历史数据处理**：学生从一个托管班级转到另一个，或中途退出托管班，其历史每日任务/评星记录是保留在原托管班级名下，还是随学生迁移到新班级——建议默认「保留在产生记录时所属的托管班级下，不随学生迁移」，退托管学生从当前看板隐藏但历史仍可查
- **任务库模板变更对历史记录的影响**：任务模板被编辑或删除后，已经下发出去的历史每日任务不应被追溯修改——建议每日任务记录保存的是任务名称等信息的「快照」，不引用模板的实时状态
- **月度统计的计算口径**：若某天老师完全没有给学生分配任务（任务列表为空），当天应算"未完成"还是不计入统计分母——需要在实现阶段明确定义，避免完成率失真
- **"放学"操作是否支持撤销**：老师误触"放学"按钮后，是否需要一个撤销入口
- **分享图片的传播风险提示**：图片海报一旦保存/分享出去即脱离系统，老师后续通过微信转发不受系统控制，仅作为已知风险记录，非功能缺陷

## 十、后续可扩展方向（v1.1+ 候选，本轮不承诺）

- **家长端只读入口**：家长通过专属链接/二维码查看孩子的当天及历史学习情况，替代目前"老师手动分享图片"的方式
- **自动化提醒**：某学生连续多天任务未完成时，自动向老师或家长提示预警
- **按学校班级维度的统计视角**：便于老师和学校老师沟通某个学校班级整体的托管完成情况（依赖已有的"学校班级"信息属性）
- **消息推送对接**：接入微信服务号/公众号等，自动推送每日报告，减少老师手动转发的操作
- **机构后台开通自助化**：机构管理员自助入驻/开通流程，替代当前"后台手动开通"
- **计费与订阅管理模块**：视商业模式落地情况，增加机构级别的订阅/计费能力（见下节）

## 十一、商业模式方向（初步，不影响 v1.0 功能范围）

- **现阶段**：先向 1~2 家托管机构免费开放试用，验证核心使用流程和留存情况，暂不涉及计费功能开发
- **第一阶段（验证后）—— 机构订阅制**：向托管机构收取年费/月费，可按学生数或托管班级数阶梯定价。选择这个方向的原因是它与现有的「机构」数据隔离架构天然吻合——机构管理员账号本身就是付费主体，无需额外设计计费归属单元
- **第二增长曲线（视留存表现再评估）—— 家长端增值服务**：老师端保持免费/低价用于机构获客，向家长提供历史报告查看、消息即时推送等付费会员功能，作为 C 端付费的第二条增长路径
- 教辅资源分销等其他方向暂不作为优先级，留待后续视业务发展评估
